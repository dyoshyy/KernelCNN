create a image 

    $ docker compose up -d

create a container

    $ docker run --volume ./app:/root/app --name {container_name} -it {image_name} bash

start a container

    $ docker start {container ID or name}

execute a container

    $ docker attach {container ID or name}

#########################################

    $ docker compose up -d --build
    $ docker exec -it exp bash